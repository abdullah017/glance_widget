<?xml version="1.0" encoding="utf-8"?>
<!-- The namespace lives in build.gradle.kts; the `package` attribute here was
     removed in AGP 8. Widget receivers are declared by the host application's
     manifest, not by this library. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
</manifest>
