# The example is an app, not a published package: API docs are noise here.
include: ../../../analysis_options.yaml

analyzer:
  errors:
    public_member_api_docs: ignore
  exclude:
    - "android/**"
    - "ios/**"
    - build/**
