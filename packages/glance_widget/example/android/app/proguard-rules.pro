# The example app's own rules. The plugin's rules arrive automatically through
# its `consumer-rules.pro`; this file exists so the release build has somewhere
# to put app-specific keeps, and so `proguardFiles` has a file to name.
